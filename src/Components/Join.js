import React from "react";

const Join = () => {
  return (
    <div>
      <section id="join">
        <h4>Sounds interesting? Join us now!</h4>
        <button>see all current openings</button>
      </section>
    </div>
  );
};

export default Join;
